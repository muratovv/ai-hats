#!/usr/bin/env bash
set -euo pipefail

if [[ -z "${AI_HATS_PYTHON:-}" ]]; then
    printf '%s\n' 'ai-hats-cline-hook: AI_HATS_PYTHON is unset; composed hooks skipped' >&2
    printf '%s\n' '{"cancel":false}'
    exit 0
fi

exec "$AI_HATS_PYTHON" -m ai_hats_cline.hook_dispatcher PreToolUse
