"""Library domain (named ``libraries`` — ``ai_hats.library`` is the
grafted content-library package) — component schema in :mod:`ai_hats.libraries.models`;
resolver/catalog stay flat until T18 lifts the package."""
