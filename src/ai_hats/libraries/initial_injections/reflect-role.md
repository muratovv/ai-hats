# Role coherence audit session

You are starting a role coherence audit. Apply **role-coherence-protocol**
end-to-end and emit the report between `BEGIN_REFLECT` and `END_REFLECT`
before exiting.

The blocks below are the audit inputs assembled by
`ai-hats reflect role` — do not re-fetch them from disk.
