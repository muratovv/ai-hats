name: dev_rule_comment_discipline
description: Default to no comment; write one only for a non-obvious WHY (one line); history is a `# HATS-NNN` pointer not a retelling; never write a stale-able count
author: ai-hats
tags: [development, code-health, dx, comments]
